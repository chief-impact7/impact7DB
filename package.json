{
  "name": "impact7db2ais",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:shared": "node scripts/check-shared-lock-sync.mjs",
    "test": "npm run test:unit && npm run test:rules",
    "test:unit": "node --test tests/class-enrollment-policy.test.js tests/promo-extractor-core.test.js tests/leave-request-sync.test.js tests/student-form-policy.test.js tests/startup.test.js",
    "test:rules": "firebase emulators:exec --only firestore --project demo-impact7 \"node --test --test-concurrency=1 tests/firestore.rules.*.test.js\"",
    "test:storage": "firebase emulators:exec --only storage --project demo-impact7 \"node --test tests/storage.rules.test.js\"",
    "upsert": "node upsert-students.js",
    "upsert:dry": "node upsert-students.js --dry-run",
    "check": "node --env-file=.env check-duplicates.js",
    "dedup": "node --env-file=.env dedup-students.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@impact7/shared": "github:chief-impact7/impact7-shared#v1.47.3",
    "@impact7/ui": "^0.10.0",
    "firebase": "^12.9.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-admin": "^13.6.1",
    "vite": "^7.3.1"
  }
}
